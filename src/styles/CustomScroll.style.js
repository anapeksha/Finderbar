import { styled } from "@mui/material/styles";
import ScrollToTop from "react-scroll-to-top";

const CustomScroll = styled(ScrollToTop)({
	color: "#e4e4e4",
	"&:hover": {
		color: "#d3d9d9",
	},
});

export default CustomScroll;
