import { Box } from "@mui/material";
import React from "react";

const ViewMovie = () => {
	return (
		<Box
			style={{
				display: "flex",
				position: "absolute",
				top: 0,
				left: 0,
				right: 0,
				bottom: 0,
				justifyContent: "center",
				alignItems: "center",
				color: "white",
			}}
		>
			ViewMovie
		</Box>
	);
};

export default ViewMovie;
